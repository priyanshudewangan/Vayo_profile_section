# ─────────────────────────────────────────────
# VAYO — Python Dependencies
# Install with:  pip install -r requirements.txt
# ─────────────────────────────────────────────

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0          # ASGI server

# Pydantic (data validation / serialisation)
pydantic>=2.6.0

# Database
asyncpg>=0.29.0                    # Async PostgreSQL driver (database.py, karma_models.py)

# Redis
redis[hiredis]>=5.0.0              # Sync client (cache.py) + async client (notifications_router.py)

# Celery (background task queue)
celery>=5.3.0
kombu>=5.3.0                       # Celery transport; pulled in transitively but pinned for stability

# Authentication (Clerk / JWT)
PyJWT[crypto]>=2.8.0               # auth.py — jwt + PyJWKClient

# AI / OpenAI-compatible clients
openai>=1.30.0                     # ai_services.py — Groq + OpenRouter via OpenAI SDK

# Environment variables
python-dotenv>=1.0.0               # load_dotenv() used across all modules

# HTTP client (used by openai SDK internally; explicit pin for stability)
httpx>=0.27.0
